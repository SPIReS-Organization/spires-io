Archived tests
==============

These tests are preserved while the SPIReS I/O sensor, config, and contract
interfaces are still in flux. They are intentionally outside the active
``tests/`` directory so the default pytest run stays sparse and focused.

Move tests back into ``tests/`` selectively as interfaces stabilize.
