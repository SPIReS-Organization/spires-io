name: Build and Test

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    name: Test on Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10', '3.11', '3.12']

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # full history for setuptools-scm

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install family deps not yet on PyPI
        # spires-contract is a runtime/test dep but not yet published; install
        # the pinned tag from GitHub. Replace with a plain PyPI spec once it
        # publishes.
        run: |
          python -m pip install --upgrade pip
          pip install "spires-contract @ git+https://github.com/SPIReS-Organization/spires-contract@v1.0.0"

      - name: Install package + test deps
        run: pip install ".[test]"

      - name: Run tests
        run: pytest -v
